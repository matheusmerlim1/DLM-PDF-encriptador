PORT=3000

# Chave mestra AES-256 (32 bytes / 64 chars hex).
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NUNCA commite o .env real.
DLM_MASTER_KEY=cole_aqui_64_chars_hex
